CCOPTS += -DDEBUG_PINS=0
SCALE ?= 1
CCOPTS += -DSCALE=$(SCALE)
SCREEN_WIDTH ?= $(shell expr $$(tput cols) / 2)
SCREEN_HEIGHT ?= $(shell tput lines)
CCOPTS += -DSCREEN_WIDTH=$(SCREEN_WIDTH) -DSCREEN_HEIGHT=$(SCREEN_HEIGHT)
ROTATE ?= 0
CCOPTS += -DROTATE=$(ROTATE)
